{
    "name": "border-relief",
    "version": "0.1.0",
    "description": "BorderRelief Donations Monorepo",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev:web": "npm run dev -w web",
        "dev:api": "npm run start:dev -w br-api",
        "build:web": "npm run build -w web",
        "build:api": "npm run build -w br-api"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}